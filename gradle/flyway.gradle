flyway {
    url = 'jdbc:postgresql://localhost:5432/postgres'
    user = 'postgres'
    password = 'password'
    schemas = ['tibia-creature-analyzer']
    defaultSchema = 'tibia-creature-analyzer'
}